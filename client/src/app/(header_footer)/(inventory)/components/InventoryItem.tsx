
type InventoryItemProps = {
  key: number;
  name: string;
  description: string;
  current_stock: number;
  unit: string;
};

export const InventoryItem = ({
  key,
  name,
  description,
  current_stock,
  unit,
}: InventoryItemProps) => {
  return (
    <div>
    </div>
  );
};
